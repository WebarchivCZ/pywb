collection:
  # Collection provides archive to collection mapping
  - /mnt/index/collections:/webarchive/collections
  # always mount the archive directory as read-only
  - /mnt/archive:/mnt/archive:ro
  # pywb configuration
  - "{{ pywb_dir }}/config.yaml:/webarchive/config.yaml"
